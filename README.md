# My slock build
Patches used:
- none

## Installation
```
git clone https://github.com/DefinitelyNotMai/slock
cd slock
sudo make install
```

## Configuration
Configure slock by making changes in "config.h", run "sudo make install" afterwards.
